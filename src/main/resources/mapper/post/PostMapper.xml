<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.vibeapp.post.PostMapper">

    <select id="findAll" resultType="Post">
        SELECT * FROM POST ORDER BY no DESC
    </select>

    <select id="findPaged" resultType="Post">
        SELECT * FROM POST 
        ORDER BY no DESC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="countAll" resultType="int">
        SELECT COUNT(*) FROM POST
    </select>

    <select id="findByNo" resultType="Post">
        SELECT * FROM POST WHERE no = #{no}
    </select>

    <insert id="save" useGeneratedKeys="true" keyProperty="no">
        INSERT INTO POST (title, content, created_at, views)
        VALUES (#{title}, #{content}, #{createdAt}, #{views})
    </insert>

    <update id="update">
        UPDATE POST
        SET title = #{title},
            content = #{content},
            updated_at = #{updatedAt}
        WHERE no = #{no}
    </update>

    <delete id="deleteByNo">
        DELETE FROM POST WHERE no = #{no}
    </delete>

    <update id="incrementViews">
        UPDATE POST SET views = views + 1 WHERE no = #{no}
    </update>

</mapper>
